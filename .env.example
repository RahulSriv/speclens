GROQ_API_KEY=gsk_...          # Required — powers the free shared tier
GEMINI_API_KEY=              # Optional — server-side fallback for Gemini BYOK users
FREE_ANALYSES_PER_DAY=3      # Optional — default is 3
